{
  "name": "esencia-hair-studio",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "npm run build && node scripts/wrangler-project.mjs dev",
    "dev:static": "node scripts/build.mjs && node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "deploy": "npm run verify:deploy && node scripts/wrangler-project.mjs deploy",
    "preview": "npm run build && node scripts/wrangler-project.mjs dev",
    "start": "node scripts/wrangler-project.mjs dev",
    "cf:auth": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/configure-cloudflare-auth.ps1",
    "cf:whoami": "node scripts/wrangler-project.mjs whoami",
    "verify:project": "node scripts/verify-project.mjs",
    "verify:push": "node scripts/verify-project.mjs --push",
    "verify:deploy": "node scripts/verify-project.mjs --deploy",
    "admin:hash": "node scripts/hash-admin-password.mjs",
    "typecheck": "wrangler types --check=false && tsc --noEmit",
    "test": "npm run build && npm run typecheck && node --test tests/site.test.mjs",
    "lint": "node --check scripts/build.mjs && node --check scripts/serve.mjs && node --check scripts/hash-admin-password.mjs && node --check js/main.js && node --check js/admin.js && npm run typecheck"
  },
  "devDependencies": {
    "typescript": "5.9.2",
    "wrangler": "4.123.0"
  },
  "type": "module"
}
